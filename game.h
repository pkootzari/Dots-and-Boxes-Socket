#include<unistd.h>
#include<string.h>

char* itoa(int number, char string[]);

int answer_validator(int answer);

int max(int x, int y);

void initiate_game(int port_nember, int numner_of_players, int my_turn);